1. Эти все  поля вы на форме будете руками вводить? это же ведь справочные данные, их можно хранить в БД и выбирать из списка
https://github.com/anton415/job4j_cars/blob/main/db/scripts/002_ddl_create_cars_table.sql#L6-L11

Статус: исправлено. Для справочных полей автомобиля добавлены enum-списки, а на форме создания объявления ручной ввод заменен на выбор из списка для марки, модели, года выпуска, кузова, двигателя и коробки передач.
Проверка: `mvn test` - успешно, 20 тестов.
Оставшиеся риски: значения справочников хранятся в коде как enum, а не в отдельных таблицах БД; это сделано как минимальное исправление под замечание.

2. Вот это контролируйте фильтрами
https://github.com/anton415/job4j_cars/blob/main/src/main/java/ru/job4j/cars/controller/PostController.java#L62-L64

Статус: исправлено. Доступ к `/posts/create` контролируется `AuthFilter`, ручная проверка пользователя из GET-метода `PostController.createPage` убрана.
Проверка: `mvn test` - успешно, 20 тестов.
Оставшиеся риски: фильтр закрывает только создание объявления; остальные замечания из этого файла не обрабатывались в рамках текущей правки.

3. Может это все прямо на форме валидировать?
https://github.com/anton415/job4j_cars/blob/main/src/main/java/ru/job4j/cars/controller/PostController.java#L139-L166

Статус: исправлено. Ручная валидация полей создания объявления удалена из `PostController`; базовые ограничения оставлены на форме через `required`, `min`, `step`, `pattern` и выбор из справочных списков.
Проверка: `mvn clean test` - успешно, 20 тестов.
Оставшиеся риски: валидация рассчитана на отправку через HTML-форму; прямой обход формы отдельной серверной валидацией в рамках этого замечания не закрывался.

4. Не вижу DTO в проекте

Статус: исправлено. Добавлен `PostDto` для формы создания объявления; `PostController` теперь принимает DTO и вручную собирает из него `Car` и `Post` перед сохранением.
Проверка: `mvn test` - успешно, 20 тестов.
Оставшиеся риски: DTO добавлен только в поток создания объявления; страницы списка и деталей по-прежнему используют сущности, чтобы не расширять правку за пределы замечания.

5. Где работа с разными фильтрами - по кухову, по дате подачи объявления, с фото???

Статус: исправлено. На странице списка объявлений добавлены GET-фильтры по кузову, дате подачи объявления и наличию фото; `PostController`, `PostService` и `PostRepository` передают эти параметры до Hibernate-запроса.
Проверка: `mvn clean test` - успешно, 20 тестов.
Оставшиеся риски: фильтр по дате ищет объявления за выбранную календарную дату; более сложные периоды не добавлялись, чтобы не расширять правку.

6. Этот вызов
https://github.com/anton415/job4j_cars/blob/main/src/main/java/ru/job4j/cars/repository/UserRepository.java#L22
нужно обернуть в try-catch и если все плохо - вернуть пустой Optional и это убираем
https://github.com/anton415/job4j_cars/blob/main/src/main/java/ru/job4j/cars/service/UserService.java#L18-L20

Статус: исправлено. `UserRepository.create` теперь возвращает `Optional<User>` и при ошибке сохранения возвращает `Optional.empty()`, а предварительная проверка email из `UserService.register` убрана.
Проверка: `mvn test` - успешно, 21 тест.
Оставшиеся риски: репозиторий скрывает любую ошибку сохранения пользователя как пустой `Optional`; это сделано по замечанию.
